{
  Type: Exception,
  Message: AsTracking() is ignored, since it is followed by Count, which returns no entity. EF only tracks entities, so tracking options do nothing on a query that returns none. Remove it.
}